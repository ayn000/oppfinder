services:
  oppfinder:
    build: .
    container_name: oppfinder
    restart: unless-stopped
    env_file: .env
    ports:
      # exposé uniquement en local — nginx fait le reverse proxy HTTPS
      - "127.0.0.1:8300:8000"
    volumes:
      - ./data:/app/data
